---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: projectspringboot                  # Ingress name
  labels:
    app: projectspringboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "projectspringboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: projectspringboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
